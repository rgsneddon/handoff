# Grok — start here on this repo

This repository is the **Mac-to-Mac operator handoff**.

1. Read **HANDOFF.md** in full before doing any product work.
2. If the user typed `/handoff` or “read the handoff”, follow `skill/handoff/SKILL.md`.
3. Status of every GitHub repo and every platform lives only in HANDOFF.md. Per-repo `WINDOWS_HANDOFF_*.md` / `APPLE_HANDOFF_*.md` files are pin-specific build recipes, not the global index.
4. The Windows box was **formatted** (2026-09-11). There is **no local repo** on that disk. Windows start is **WINDOWS.md** (`https://github.com/rgsneddon/handoff/blob/main/WINDOWS.md`). Use `gh repo clone` after `gh auth login`. Do not `git -C` a missing path. Do not recut wallet 0.30 or ShearK 1.6.

Account: `rgsneddon`. Private repo. No secrets in git.
